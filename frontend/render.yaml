services:
  - type: static
    name: biteme-frontend
    plan: free
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://biteme-backend.onrender.com
      - key: NODE_ENV
        value: production
